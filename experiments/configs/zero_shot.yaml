# Zero-shot SAM baseline - COMPLETELY FROZEN
experiment_name: zero_shot
dataset: isic
data_root: data
sam_checkpoint: checkpoints/sam_vit_b_01ec64.pth
model_type: vit_b
seed: 42

# Core settings - EVERYTHING FROZEN
freeze_encoder: true
freeze_decoder: true
freeze_prompt_encoder: true
use_prompts: false
use_adapters: false

# Training (evaluation only)
num_epochs: 0
batch_size: 4
target_size: [1024, 1024]
num_workers: 4